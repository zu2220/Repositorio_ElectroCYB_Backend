spring.application.name=electrocyb_backend

# ----------------------- DATABASE (use env vars in production) -----------------------
# You can override these in Render as DB_URL, DB_USERNAME, DB_PASSWORD
spring.datasource.url=${DB_URL:jdbc:postgresql://innovacionserver.postgres.database.azure.com:5432/postgres?user=postgres&password=Innovacion123&sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Innovacion123}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA settings - change DDL_AUTO to 'validate' in production environment variables
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:true}
logging.level.org.hibernate.SQL=${SQL_LOG_LEVEL:debug}
logging.level.org.hibernate.type.descriptor.sql=${HIBERNATE_LOG_LEVEL:trace}
spring.jpa.properties.hibernate.format_sql=${FORMAT_SQL:true}

spring.web.locale=es_PE
server.port=${SERVER_PORT:8080}

# ----------------------- EMAIL (use env vars in production) -----------------------
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:electrocyb@gmail.com}
spring.mail.password=${MAIL_PASSWORD:lnam aanv vicv djgh}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8

# ----------------------- OpenAI (use env vars) -----------------------
openai.api.key=${OPENAI_API_KEY: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}
openai.base.url=${OPENAI_BASE_URL:https://api.openai.com/v1}
openai.model=${OPENAI_MODEL:gpt-4o-mini}

# ----------------------- JWT -----------------------
jwt.secret=${JWT_SECRET:una_clave_super_secreta_y_larga_para_jwt_1234567890}
jwt.expiration=${JWT_EXPIRATION:14400000}

# ----------------------- CORS -----------------------
# Comma-separated list of allowed origins. Example:
# app.cors.allowed-origins=http://localhost:5173,https://your-site.netlify.app
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}
